name: netsentinel
version: 2.1.36
description: >
  NetSentinel — Enterprise network security scanner, rogue device detector,
  and connectivity monitor. Professional enterprise UI built with PyQt6.

targets:
  - claude

dependencies:
  apm: []

# Coding rules, architecture, and project vision live in .apm/instructions/*.md
# (the compiler's sole source). They are NOT duplicated here — a YAML rules:
# block in this file is not read by `apm compile` and only rots out of sync.

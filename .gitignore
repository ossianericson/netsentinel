# Claude Code session memory (local AI assistant state — not project code)
.claude/settings.local.json
.claude/todos/
.claude/*.md

# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python
*.egg-info/
dist/
build/
# NetSentinel.spec is intentionally tracked — it is the hand-crafted PyInstaller config

# Virtual environments
.venv/
.venv*/
venv/
env/

# Build debug logs
build_debug.log
build_*.log

# PyInstaller — ignore auto-generated specs but track our hand-crafted one
# *.spec  ← removed; NetSentinel.spec is the source-of-truth build config

# IDE
.vscode/settings.json
.idea/
*.swp

# OS
.DS_Store
Thumbs.db

# mypy cache
.mypy_cache/

# Test coverage
.coverage
coverage.xml
htmlcov/

# Reports (generated by the tool — don't commit user data)
netsentinel_report_*.html


# APM dependencies
apm_modules/

# Runtime user data — never commit
NetSentinel.db
NetSentinel.db-shm
NetSentinel.db-wal
NetSentinel.ini

# Runtime log files written by the app — never commit
startup_log.txt
netsentinel_debug*.log
netsentinel_crash.log
netsentinel_stderr.log

# Generated output
repomix-output.txt
release_body.md

# One-shot migration/patch scripts (run once during development, not for contributors)
tools/_*.py
tools/extract_*.py
tools/find_orphan_decorators.py
tools/remove_orphan_decorators.py
tools/fix_ax1.py

# Test/chaos tester output — screenshots, crash reports, logs
test_output/

# Test runner output files (monkey test, run_tests_*.bat, ad-hoc manual runs)
inventory_events.csv
last_run.txt
test_results*.txt
test_out.txt
test_full_results.txt
test_results_verbose.txt
TEMP*.json

